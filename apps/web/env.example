# Web app environment variables (example)
# Copy values into apps/web/.env.local for local development

# Clerk
CLERK_SECRET_KEY="sk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# Clerk JWT template issuer domain (used by both web and Convex)
# Example: https://your-app.clerk.accounts.dev or https://clerk.your-domain.com
CLERK_JWT_ISSUER_DOMAIN="https://<your_clerk_issuer_domain>"

# Convex
# Get your deployment URL from the Convex dashboard after running `bun dev:setup` or creating a project.
NEXT_PUBLIC_CONVEX_URL="https://<your-convex-deployment>.convex.cloud"
# Optional: e.g., "dev" or "prod" for your own bookkeeping
CONVEX_DEPLOYMENT="dev"

# AI (Gemini) — used server-side only
GOOGLE_GEMINI_API_KEY="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
